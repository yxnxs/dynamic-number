fn main() {
    //    let target_value = 49534;
    //    let numbers = vec![1, 3, 7];
    //
    // {
    //     let start = std::time::SystemTime::now();
    //     let result = recursion(target_value, &numbers);
    //     let delta = start.elapsed().unwrap().as_secs();
    //     println!("Recursion result in {delta}s: {result}");
    // }

    //{
    //    let start = std::time::SystemTime::now();
    //    let mut table = vec![-1; target_value + 1];
    //    let result = top_down(target_value, &numbers, &mut table);
    //    let delta = start.elapsed().unwrap().as_secs();
    //    println!("Top Down result in {delta}s: {result}");
    //}

    //{
    //    let start = std::time::SystemTime::now();
    //    let result = bottom_up(target_value, &numbers);
    //    let delta = start.elapsed().unwrap().as_millis();
    //    println!("Bottom Up result in {delta}ms: {result}");
    //}

    println!("This project is meant to demonstrate a benchmark\n. Please run\n\n\tcargo bench\n\nto perform the benchmark");
}
